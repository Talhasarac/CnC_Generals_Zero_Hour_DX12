/* The stub's miles.c is the W3D-flavored Thyme variant and expects the same
 * declarations the game compiles against - use the one true header.  Relative
 * path on purpose: "MSS/MSS.h" would resolve back to this very file. */
#include "../../include/MSS/MSS.h"
